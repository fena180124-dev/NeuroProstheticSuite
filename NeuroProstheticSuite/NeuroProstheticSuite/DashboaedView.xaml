<UserControl x:Class="NeuroProstheticSuite.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto" Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Top">
            <TextBlock Text="Dashboard Sinyal (Real-Time)" FontSize="20" FontWeight="SemiBold" Foreground="LightGray" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,0,0,0">
                <Button x:Name="BtnStartStop" Content="Stop" Width="80" Height="26" Margin="12,0,0,0" Click="BtnStartStop_Click"/>
                <TextBlock Text="  Update ms:" Foreground="#BBB" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBox x:Name="TxtInterval" Width="60" Height="22" Text="50" Margin="6,0,0,0" VerticalAlignment="Center" TextAlignment="Center"/>
                <Button Content="Set" Width="48" Height="22" Margin="6,0,0,0" Click="BtnSetInterval_Click"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="200"/>
            </Grid.RowDefinitions>

            <!-- Top: EMG realtime -->
            <Border Grid.Row="0" Background="#111" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="EMG (Simulated Real-Time)" Foreground="#DDD" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Canvas x:Name="CanvasEmg" Height="260" Background="#0B0B0B" SnapsToDevicePixels="True"/>
                </StackPanel>
            </Border>

            <!-- Bottom: FSR realtime -->
            <Border Grid.Row="1" Background="#111" CornerRadius="6" Padding="10">
                <StackPanel>
                    <TextBlock Text="FSR (Simulated Pressure)" Foreground="#DDD" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Canvas x:Name="CanvasFsr" Height="140" Background="#0B0B0B" SnapsToDevicePixels="True"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>